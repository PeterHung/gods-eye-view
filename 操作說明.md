# God’s Eye View：一鍵部署與操作

已於 2026-09-17 在本機 macOS 實際部署並驗證。服務目前已啟動。

網頁入口：http://127.0.0.1:4173

腳本所在資料夾：

```text
/Users/peterhung/Documents/Codex/Project/2026-09-17_gods-eye-view-analysis_gpt-6_reasoning-unspecified
```

## 使用方法

在 Finder 打開上述資料夾，雙擊對應 `.command` 檔即可。終端機視窗會顯示操作結果，按 Enter 關閉視窗。服務由使用者工作階段的 launchd 管理，關閉終端機或瀏覽器不會停止服務。

| 腳本 | 行為 |
|---|---|
| `01-一鍵部署.command` | 下載並核對專用 Node.js，依 package-lock 安裝依賴、驗證建置、啟動服務及開啟網頁；可重複執行 |
| `02-啟動.command` | 啟動已部署服務並開啟網頁；重複執行不建立第二個服務 |
| `03-停止.command` | 停止本專案服務；已停止時可安全重複執行 |
| `04-移除安裝.command` | 先停止服務，再刪除 `.runtime`、`source/node_modules`、`source/dist` 與 `logs` |
| `05-查看狀態.command` | 顯示是否安裝、服務是否載入及 HTTP 是否就緒 |

移除安裝會保留 `source/` 原始碼、`source/.env` 等設定檔、分析報告、驗證紀錄及腳本。若要再次使用，重新執行一鍵部署即可。這不是刪除整個專案的腳本。

## 可以透過網頁做什麼

本次實際用瀏覽器確認：

- 地球與 Esri 影像正常顯示。
- 首次使用選單可選 EXPLORE MANUALLY。
- Reset Globe 可回到完整地球畫面。
- DATA LAYERS 面板能展開，Earthquakes 圖層可切為 ON。
- USGS 圖層成功載入 28 筆事件，地球上顯示規模標籤。數量會隨時間改變。
- POWER UP 可以開啟 Provider Settings，顯示各服務的 API 金鑰欄位與 SAVE KEYS 按鈕。

尚未輸入或儲存真實金鑰，未驗證需金鑰的擬真 3D、語音、船舶及火點功能。網頁操作的是應用功能與供應商設定；服務本身的部署、啟動、停止與移除由上述本機腳本管理。

## 執行方式

使用專案內 `.runtime/node/` 的 Node.js **24.21.0**，不替換系統 Node.js。Apple Silicon 與 Intel macOS 的官方壓縮檔 SHA-256 都已固定在管理腳本；本機實測為 Apple Silicon。依賴使用 `npm ci --include=dev`，不額外下載 Puppeteer 瀏覽器。

部署包含 `npm run build` 驗證，但啟動採 Vite 的本機開發模式，因上游金鑰編輯面板僅在此模式啟用。服務限定 `127.0.0.1:4173`，是本機部署；不是公網或其他電腦可直接存取的部署。它沒有設定開機／登入自動啟動，登出或重開機後請再執行「啟動」。

日誌：`logs/server.log`。若埠號被其他服務使用，腳本會報錯，且不會停止其他程式。請先關閉占用該埠的程式，再重新啟動。

若要搬動或重新命名資料夾，請先執行「停止」，搬動完成後再執行「啟動」。管理腳本以自身位置解析路徑，並以專案路徑衍生獨立服務名稱。

## 驗證結果

- Node.js 24.21.0 安裝及 SHA-256 驗證成功。
- 依賴安裝與 Vite 建置成功；上游建置有大型 chunk 提醒，不影響建置通過。
- Setup doctor 回報 Ready；未配置 API 金鑰符合免金鑰使用方式。
- 四個抽樣測試檔合計 **50 項通過、0 失敗**，包含先前未安裝 Cesium 而無法完成的測試。
- 啟動、重複啟動、停止後重新啟動與 HTTP 就緒檢查通過。
- 4173 被測試 listener 占用時拒絕啟動，保留既有 listener。
- 管理操作的並行鎖測試通過。
- 移除在隔離測試資料夾驗證：刪除安裝產物，同時保留原始碼、`.env`、報告與證據；重複移除通過。正式安裝保留可使用狀態。

完整紀錄在 `evidence/deployment-tests.txt`、`evidence/doctor-deployment.txt`、`evidence/launcher-tests.txt` 與 `evidence/deploy.log`。

## 介面語言切換

上方工具列的地球圖示右側提供與地球圖示同尺寸的圓形按鈕；顯示「中／EN」表示目前語言，點一下可切換繁體中文與英文，切換立即生效且重新整理後會保留選擇。首次使用依瀏覽器語言設定預設介面；無法使用瀏覽器儲存空間時，當次切換仍可使用。

翻譯涵蓋主要面板、圖層與狀態、位置搜尋提示、視覺樣式、首次使用畫面、語音控制標籤及金鑰設定。地名、航班代碼、供應商名稱、來源資料與戰術 HUD 遙測保留原文；此功能不會改變語音模型的回覆語言。

已驗證中英切換、兩種語言重新整理後的記憶、中文動態圖層開關、金鑰面板及桌面／窄螢幕按鈕位置。相關 19 項單元測試通過，建置成功。

## 部署版的服務設定入口

右下角「設定服務」在本機與部署版本都會保留。本機 Vite 開發伺服器可直接填入並儲存金鑰。線上反向代理部署需先在主機啟用網頁編輯，之後用管理密碼解鎖，才能出現金鑰輸入欄與「儲存金鑰」。靜態網站或尚未啟用的部署只顯示設定說明，不會收集金鑰。

在部署主機的 `source/` 目錄執行：

```text
node scripts/enable-hosted-settings.mjs https://hermes.petersjobs.dpdns.org/gods-eye/
```

請改成實際 HTTPS 網址。指令會寫入 `GEV_SETTINGS_ORIGIN` 與管理密碼雜湊，並只顯示一次明文密碼；請立即保存。完整停止並重新啟動 Node 服務後，重新整理網頁，開啟「設定服務」，貼上管理密碼解鎖，再貼上各服務 API 金鑰並儲存。關閉面板會再次鎖定。再次執行指令會更換管理密碼，既有 API 金鑰不變。

反向代理必須把 `/gods-eye/api/setup/` 轉到 Vite，並覆寫 `Host`、`X-Forwarded-Host` 與 `X-Forwarded-Proto`。設定 API 相對於目前頁面目錄請求。已儲存的金鑰不會回傳到瀏覽器；欄位留空會保留原值。Google Maps 與 Cesium ion 金鑰會在瀏覽器使用，儲存後需重新載入頁面才套用。

2026-09-18 驗證：41 項測試通過；正式建置與 import 邊界檢查通過；以本機 production preview 實測缺少設定 API 時仍顯示設定面板、沒有輸入或儲存控制。子目錄設定 API 路徑另有單元測試。直接以 Vite `--base=/gods-eye/` 建置的全站測試發現上游 Cesium 資源載入問題，因此未宣稱完整子目錄靜態部署已通過；現有線上服務使用反向代理的 Vite 執行方式。

線上另觀察到 Vite WebSocket 更新連線失敗。上傳 GitHub 後，Hermes 主機仍需拉取新版本並重啟或重新部署，然後手動重新整理網頁。

2026-09-18 後續：部署版金鑰面板改為管理密碼解鎖後才顯示輸入欄。隔離 UI 測試確認錯誤密碼無法解鎖、正確密碼顯示 9 個金鑰欄位與儲存按鈕、儲存後欄位清空並可重新鎖定，中英切換同步更新解鎖／儲存文案。相關 16 項單元測試通過。線上 Hermes 仍需拉取新版本、在主機執行啟用指令、完整重啟 Node 後才會出現輸入欄。
線上頁若右下角「設定服務」消失，多半是啟動過程把入口拆掉，或模板沒注入。現在設定面板會在缺少標記時自行建立，啟動中止也只關閉對話框、保留按鈕。拉取 `00bd492` 之後的更新並重啟 Hermes 後，重新整理即可。
